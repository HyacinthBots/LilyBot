package net.irisshaders.lilybot.database.migrations.main

import org.litote.kmongo.coroutine.CoroutineDatabase

@Suppress("UnusedPrivateMember")
suspend fun mainV1(db: CoroutineDatabase) {
	// There is nothing to do here currently. This is to shut up the compiler until we eventually need to migrate
	// something in main
}
